# @exactjs/accessibility guidance

Use this package when a custom component needs ref-based ARIA relationships, focus
entry/restoration, or a complete supported composite-keyboard pattern. Prefer native HTML behavior
and ordinary ARIA attributes first. Register the enhancement package-wide when all components
should receive its diagnostics; otherwise use a local attributed namespace import. See the package
README and framework accessibility reference for the supported surface and limits.
