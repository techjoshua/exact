# @exactjs/react-compatibility

Repository-only compatibility corpus for comparing eXact's React layer with supported React
versions.

## Purpose

The workspace contains conformance fixtures and shared test inputs. It is not an application
dependency or a public compatibility entry point. Applications should use
`@exactjs/react-compat` through an eXact build integration.
