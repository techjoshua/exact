// Hydration tests render raw, uncompiled component fixtures through the installed SSR package.
// Install the generic capability that a compiled fallback artifact would import in production.
import '@exactjs/ssr/runtime/generic-components';
