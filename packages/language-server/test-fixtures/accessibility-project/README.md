# Accessibility language fixture

Private integration fixture proving that a package-scoped accessibility enhancement export activates
the trusted provider through the generic eXact language-extension host.
