/** Provides the canonical proxy marker value. */
export const proxyMarker = Symbol.for('exact.reactive.proxy');
/** Provides the canonical reactive value marker value. */
export const reactiveValueMarker = Symbol.for('exact.reactive.value');
/** Provides the canonical raw target value. */
export const rawTarget = Symbol.for('exact.reactive.raw');
/** Provides the canonical reactive value ref value. */
export const reactiveValueRef = Symbol.for('exact.reactive.valueRef');
/** Provides the canonical iterate key value. */
export const iterateKey = Symbol.for('exact.reactive.iterate');
