/** Provides the canonical exact server manifest version value. */
export const exactServerManifestVersion = 1 as const;
/** Provides the canonical exact compiler manifest version value. */
export const exactCompilerManifestVersion = 1 as const;
