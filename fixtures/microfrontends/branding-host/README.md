# Branding host fixture

Minimal private branding remote used by eXact microfrontend integration tests. It exercises shared
context and remote-entry behavior without the complexity of the full portal sample.

Keep source and package metadata deterministic so generated artifact comparisons remain stable.
