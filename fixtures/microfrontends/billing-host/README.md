# Billing host fixture

Minimal private microfrontend host used by compiler, bundler, and server integration tests. It
represents a billing remote with deterministic exposures and manifests.

The fixture is not a standalone sample or publishable package; alter it only alongside the
integration expectations that consume its artifacts.
