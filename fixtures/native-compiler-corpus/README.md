# Native compiler corpus

Repository-only TypeScript and TSX fixtures that retain high-value semantic edge cases from the
retired expression-projection laboratory. The native corpus check compiles these sources directly;
they are not an application, workspace package, or published API.
