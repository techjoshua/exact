# Using `@exactjs/forms`

See the [README](./README.md) for setup and examples. Use this package for accessible, eXact-native
form composition with coordinated validation and submission state.

- Keep application values and server errors in component state.
- Use `Form` as the submission interaction host and `Submit` for pending presentation.
- Use field components for labels, descriptions, constraints, and accessible error relationships.
