# @exactjs/theme-fixture

This private component library is the package-boundary acceptance fixture for `@exactjs/theme`. It exports a representative native-control specimen, a compound range control, and an accessible translucent area chart. The package owns no CSS and consumes only the public semantic enhancement and derivation contracts.
