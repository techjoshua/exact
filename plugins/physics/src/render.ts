import type { ExactRuntimePluginExtension } from '@exactjs/plugin-api';

/** Creates the renderer-neutral physics runtime extension. */
export default function createPhysicsRenderExtension(): ExactRuntimePluginExtension {
	return Object.freeze({});
}
