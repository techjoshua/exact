import type { ExactRuntimePluginExtension } from '@exactjs/plugin-api';

/** Creates the browser-safe physics runtime extension. */
export default function createPhysicsClientExtension(): ExactRuntimePluginExtension {
	return Object.freeze({});
}
