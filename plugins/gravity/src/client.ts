import type { ExactRuntimePluginExtension } from '@exactjs/plugin-api';

/** Creates the browser-safe gravity runtime extension. */
export default function createGravityClientExtension(): ExactRuntimePluginExtension {
	return Object.freeze({});
}
