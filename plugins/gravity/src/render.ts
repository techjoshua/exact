import type { ExactRuntimePluginExtension } from '@exactjs/plugin-api';

/** Creates the renderer-neutral gravity runtime extension. */
export default function createGravityRenderExtension(): ExactRuntimePluginExtension {
	return Object.freeze({});
}
