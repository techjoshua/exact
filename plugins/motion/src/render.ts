import type { ExactRuntimePluginExtension } from '@exactjs/plugin-api';

/** Creates the renderer-neutral motion plugin lifecycle extension. */
export default function createMotionRenderExtension(): ExactRuntimePluginExtension {
	return Object.freeze({});
}
