import type { ExactRuntimePluginExtension } from '@exactjs/plugin-api';

/** Creates the renderer-neutral gesture runtime extension. */
export default function createGestureRenderExtension(): ExactRuntimePluginExtension {
	return Object.freeze({});
}
