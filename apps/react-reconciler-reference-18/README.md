# React Reconciler 18 reference fixture

Private renderer fixture for the React 18-compatible `react-reconciler` line. It records host
config and renderer behavior used by eXact's reconciler compatibility checks.

The package is intentionally version-pinned and should not be used by application code.
