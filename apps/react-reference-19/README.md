# React 19 reference fixture

Private reference application pinned to React 19. It provides upstream runtime observations used
to validate eXact's compatibility implementation.

Keep the fixture independent from eXact runtime packages so differential results remain credible.
