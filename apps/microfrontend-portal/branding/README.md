# Branding microfrontend sample

Private remote package used by the microfrontend portal sample. It provides branding context and
UI consumed across host boundaries and exercises shared eXact package negotiation.

Run development and production workflows from the parent portal package so all remotes use the
same generated manifests and entry registry.
