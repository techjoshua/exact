# Page microfrontend sample

Private page host for the microfrontend portal sample. It composes remote billing and branding
capabilities, owns the document shell, and demonstrates server/client microfrontend coordination.

Use the parent portal scripts to generate artifacts, build remotes, start servers, and run the
integration tests.
