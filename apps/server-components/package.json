{
	"name": "@exactjs/sample-server-components",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"pregenerate": "npm run build -w @exactjs/compiler",
		"generate": "node scripts/generate-artifacts.mjs",
		"build": "npm run generate && tsc --noEmit",
		"test": "npm run generate && vitest run"
	},
	"dependencies": {
		"@exactjs/core": "^0.5.0",
		"@exactjs/hydrate": "^0.5.0",
		"@exactjs/jsx": "^0.5.0",
		"@exactjs/server": "^0.5.0",
		"@exactjs/ssr": "^0.5.0"
	},
	"devDependencies": {
		"@exactjs/vitest": "^0.5.0",
		"typescript": "^7.0.2"
	}
}
