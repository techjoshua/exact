# React Reconciler 19 reference fixture

Private renderer fixture for the React 19-compatible `react-reconciler` line. It supports
differential checks of host configuration, scheduling, commits, and public renderer behavior.

Keep version changes explicit because the package exists to preserve a stable upstream baseline.
