# React 18 reference fixture

Private reference application pinned to React 18. It captures expected React runtime behavior for
eXact compatibility and conformance comparisons.

This package is test infrastructure. Do not add eXact substitutions or application features here;
changes should remain narrowly tied to a documented compatibility observation.
